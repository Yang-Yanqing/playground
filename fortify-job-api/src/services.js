import { handleStatus,handleTransition } from "./domain";

const createJob=(jobName)=>{
  if(!jobName){console.error("Name of job is required")}
  try {
    const jobId=null;
    
  } catch (error) {
    
  }
}